import React from "react";

function ConnectionSkill({ skill }) {
  return (
    <span className="bg-blue-500 text-white px-3 py-1 rounded-full text-xs font-semibold">
      {skill}
    </span>
  );
}

export default ConnectionSkill;
